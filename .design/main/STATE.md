# Project State

<!-- STATE.md — live project memory. Read FIRST in every workflow session. -->
<!-- Maximum 100 lines. Agent updates AFTER each completed action. -->

**Workspace:** main
**Updated:** 2026-09-19 13:49
**Phase:** 32 — Command-Surface Audit Closure
**Status:** Done

## Current Position

- **Task:** T-32T01 Full-cycle validation — Phase 32 complete
- **Next Action:** Plan complete — run /magic.task main to plan new scope

## Progress

```
Overall: [31/31] ████████ 100%
```

## Recent Decisions

<!-- Last 3-5 locked decisions. Older entries are dropped (not archived) — see PLAN.md / CHANGELOG.md for phase history. -->

- 2026-09-19 **Decision:** `/magic.run main` — **T-32T01 Done: PHASE 32 COMPLETE, 9/9 tasks, all five tracks.** The global `/` key focuses the TUI command bar; `ExtensionRegistry` persists across invocations; `ext add` refuses a scan-unsafe manifest and `ext activate` needs an explicit grant; `ToolGuard` flags `$(` and line breaks like the scanner; every `--format json` site emits valid JSON through one escaper; the Board Archive column reads real data with row-granular degradation. Gates: fmt clean, clippy -D warnings clean, workspace tests green on two consecutive runs; the audit's own repro now refuses at `ext add`. Disclosed, not fixed: shared-temp-root flake in a pre-existing simulation test, one stale simulation scenario, no scenario yet for the command-bar key, no integrity protection on the extension store, no `--force` override.
- 2026-09-09 **Decision:** `/magic.task main` — **Registry sync + historical correction, no new phase (v2.72.0).** Pre-flight `ok:true` (290 specs, 290 Stable, 0 Draft; engine `.magic/.version` 2.1.80 == snapshot; Rules Parity RULES v1.6.0 == TASKS Based-on-RULES; registry unchanged at INDEX v1.0.193, matching PLAN.md's own already-current `Based on` pointer — no new spec content since the last sync). Pre-Planning Stabilization a genuine no-op (0 Draft). **Found and fixed a real, disclosed staleness the phase-closure left behind**: PLAN.md's own Phase 27/28/29 section headers never gained the `— Done` marker every completed phase since 16 carries; Phase 27's own section text and a Risk-section note both still predicted "corrected in Phase 29" for the four residual behaviours — a claim Phase 29's own planning had already disproven, but which this Overview-level prose never caught up to. Corrected all of it in this pass rather than leaving the archived record to contradict the plan's own narrative: three headers marked `— Done`; both stale predictions rewritten with `[CORRECTED]` markers naming what actually happened. TASKS.md's own Overview paragraph (line 24, "27 (root + CLI) → 28 (TUI + single binary) → 29 (desktop + residual corrections)") carried the identical stale claim — corrected there too, in T-29T01's own closing pass. Added one consolidated `Revision v2.72.0 (milestone sync)` paragraph closing out Phases 27/28/29 together, since none of the three ever received its own milestone-sync entry when it actually closed (each closure lived only in TASKS.md's decision log and its own phase file) — catching up three silent gaps in one entry rather than leaving them unaddressed or writing three separately. **No new phase opened, and none is mechanically available to open**: every currently-Stable spec is built, the Backlog is empty (0 RFC, 0 Draft) — the same "nothing left to plan" state the Risk section's own v2.68.0 note already predicted this exact scenario would look like. Computed next step (DA-6): none to compute mechanically — the pipeline's next motion is authorship (`/magic.spec`, a human decision about what Cronus builds next), narrated as a fact rather than proactively invoked (magic.md §5 — `/magic.spec` is never a workflow this agent starts on its own). `node .magic/scripts/executor.js generate-context --workspace=main` and `export-wiki` both run after the write-back. (Revert: `git restore .design/main/{PLAN,TASKS,STATE}.md`)
- 2026-09-09 **Decision:** `/magic.run main` — **T-29T01 Done: PHASE 29 COMPLETE, 7/7 tasks, all four tracks.** Full quality gates: `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo build -p cronus-desktop` succeeds. `cargo test -p cronus-desktop --all-targets` (PowerShell) re-run at this closing task's own full scope, not merely trusted from earlier per-task checkpoints — fails to *load* with the same `STATUS_ENTRYPOINT_NOT_FOUND` every `cronus-desktop` test binary has carried since T-29A01, confirmed environmental rather than assumed. `pnpm -C packages/ui test` 181/181; `pnpm -C packages/ui build` clean; `fallow audit --changed-since HEAD` reported zero issues on every checkpoint run this phase, one pre-existing, unrelated `BuildingShell` complexity finding correctly excluded by the tool's own gate as inherited each time. **`rg`-audited every phase-touched Rust file for `unwrap()`/`panic!()`/`.expect()` line-by-line, not by file-level totals**: every hit resolves to either pre-existing code this phase never touched, or this phase's own `#[cfg(test)]`-gated test code — zero new production-path introductions. **Phase-wide containment sweep across all 20 touched files (Rust + TypeScript)** found and fixed one real leak this phase itself introduced (`packages/ui/src/shared/catalog.ts`'s own spec-filename mention, from T-29A03), and found — but explicitly did **not** silently fix — two genuinely pre-existing leaks outside this phase's own touched-lines scope (`crates/tui/src/command.rs:78`, `packages/ui/src/shell/conformance.test.tsx:2,42`, both predating this phase); recorded both via `record-diagnostic` rather than reaching outside this task's own scope to edit files this phase never modified. **Both disclosed exceptions verified explicitly by name against real source, not trusted from Track A's own scope line**: `capability_version`/`capability_status` still call `bridge.version()`/`bridge.status()` unchanged; `core_bridge()` still constructs `Bridge::new(Engine::new(), Vec::new(), …)` — F-5's empty-secret-list residual still genuinely open. **The Phase Notes' own renarrowing held all the way through execution**: confirmed this phase's own touched-file list never includes `crates/core/src/invocable_bootstrap/*.rs` or any `crates/cli` file — none of the four disclaimed residual corrections (unavailable-as-emptiness, the output-format flag, unescaped structured output, the empty-secret-list fix) were touched, exactly as the renarrowing at planning time said they would not be. Computed next step (DA-6, §5 Post-Task Replan): phase complete → the one user-visible next step is `/magic.task main`, never `/magic.spec`/`/magic.analyze` proactively. (Revert: `git restore packages/ui/src/shared/catalog.ts .design/main/{TASKS,STATE}.md .design/main/tasks/phase-29.md`)
- 2026-09-09 **Decision:** `/magic.run main` — **T-29C01 Done: corpus registration from the detached workspace; findings F-1 and F-7 close (6/7).** New `apps/desktop/tauri/src/conformance_registration.rs`: `DesktopProjection` wraps a real `CoreBridge` (built over an isolated, fixture-only registry+dispatcher pair, empty secret list matching production's `core_bridge()` exactly) and implements `SurfaceProjection` through `Bridge::catalog()`/`Bridge::invoke()` exclusively. **Corrected the task's own Scope text against the spec module's own clearer statement**: Scope named "the same `InvocableRegistry`/`Dispatcher` pair T-29A01 built" as what to implement the trait over; `crates/conformance/src/projection.rs`'s own module doc explicitly forbids exactly that ("the desktop shell... links no invocable registry or dispatcher at all... every surface answers through `SurfaceProjection` — a trait a live registry **and an IPC bridge** can implement identically") — read directly before writing any code, and followed. **A real gap closed in `cronus-contract`**: `SurfaceProjection::invoke` carries `&ArgValues` (no public iterator existed — no in-process caller ever needed one), but `Bridge::invoke` needs the wire's flat `HashMap<String, ArgValue>` shape; new `ArgValues::iter()` bridges the two, the minimal addition, with its own 2 tests. **Verified the corpus run's real behavior despite being unable to execute the actual test on this host**: `cronus-desktop`'s test binary carries the same disclosed `STATUS_ENTRYPOINT_NOT_FOUND` class every one of its tests has since T-29A01; reimplemented `Bridge::catalog()`/`Bridge::invoke()` and this task's own `DesktopProjection` in a throwaway, Tauri-free scratch binary (no WebView2 linkage) built from the same real crates and ran it directly — exactly the 2 disclosed redaction-residual fixtures (`secret-bearing`, `boundary-crossing`) diverged, nothing else; discarded after use. **F-1 closed as a genuine, disclosed audit, not a rubber stamp of the task text's own optimistic framing**: its own Verify-line parenthetical implied only `consumer_registered` remained open, but `copies_deleted`/`deletion_pinned` were still literally `false` in `seed_inventory()`, with no tombstone ever recorded for F-1's own 3 named sites (the command line's compile-time enum, the terminal UI's slash-catalog constant — both distinct artifacts from F-2's/F-3's own already-tombstoned ones). Verified directly, not assumed, that both are genuinely and permanently gone (checked the CLI's own current `cli.rs` doc comment and the terminal UI's own closing record for its catalog-derivation task); added the 3 missing tombstones this audit found (2 backdating real, already-permanent deletions from earlier phases' own work; 1 new for the desktop's own site — its per-capability IPC list ceasing to be the surface's only way to reach a core capability, not a claim the 4 original commands were literally deleted). **F-7 closed on different, explicitly-disclosed grounds**: a `Preemptive` finding whose risk never materialized has no artifact to physically delete, so its `copies_deleted`/`deletion_pinned` read `true` because the absence is now structural — proven by T-29B01's own "not a second catalog" regression tests, not by a deletion; named as a different kind of evidence from F-1's case, not silently treated the same. Repayment-visibility test renamed to `exactly_f1_f2_f3_and_f7_read_as_repaid_after_the_desktop_shells_own_registration`. `cargo test -p cronus-contract --all-targets` 31/31; `cargo test -p cronus-conformance --all-targets` 16/16; `cargo build`/`clippy --all-targets -- -D warnings`/`cargo fmt --all -- --check` (PowerShell) clean for `cronus-desktop`; full main-workspace `cargo test --workspace -j 2`/`clippy --workspace --all-targets -- -D warnings`/`cargo fmt --all -- --check` green. Containment self-check caught and fixed two real leaks (literal `l2-surface-conformance.md` filename mentions — an established-but-still-forbidden pattern elsewhere in this codebase, not repeated here) before landing. Computed next step (DA-6): T-29T01 (behaviour-preservation proof and full quality gates — the phase's own final task, the only one left). (Revert: `git restore crates/contract/src/lib.rs crates/conformance/src/findings.rs apps/desktop/tauri/{Cargo.toml,src/lib.rs} .design/main/{TASKS,STATE}.md .design/main/tasks/phase-29.md && rm apps/desktop/tauri/src/conformance_registration.rs`)
- 2026-09-09 **Decision:** `/magic.run main` — **T-29B02 Done: the palette's actions group renders from the catalog projection (5/7).** **Zero production code changed** — `command-palette.tsx`'s `commandPaletteDelegate` was already generic over any plain `{id, label, binding, run}` list, sourced today from `building-shell.tsx`'s `registry.live(CONTEXT_STACK)`, the exact pipeline T-29B01 made catalog-capable through the existing `actions` prop; there was no hand-written "semantic entries" list left to remove. Read `command-palette.tsx` in full before writing anything, to verify finding F-7's own premise directly rather than trust the Scope's framing: F-7 names the risk as *becoming* a fourth hand-written copy, not describing one that already existed — confirmed it still hasn't. The real, concrete gap this task closes is a missing proof, not missing wiring: 2 new tests in `overlays.test.tsx` (the file already establishing render-based `CommandPalette` conventions) drive the full real pipeline end to end — `actionsFromCatalog` → `createActionRegistry` → `.live()` → the same plain-object mapping `building-shell.tsx` performs → `<CommandPalette actions={…}>` — rather than calling `commandPaletteDelegate` in isolation, a stronger, more realistic proof matching this task's own Verify-line phrase "the palette's own consumption." A fed-in `Semantic` descriptor renders a row bearing its own `name` and dispatches on click; an omitted one produces no row at all. **F-7's own `Repayment` ledger entry in `crates/conformance/src/findings.rs` stays untouched here** — T-29C01's own Scope names flipping it (alongside F-1) as its own job, once the desktop's corpus registration proves the generated client's shape too; recording it here would pre-empt that task's own audit. Containment self-check caught and fixed one real leak (a bare task-id in the new test block's own doc comment) before landing. `pnpm -C packages/ui test` 181/181 (179 pre-existing + 2 new); `pnpm -C packages/ui build` clean; `biome check` clean after one auto-format pass. Computed next step (DA-6): T-29C01 (corpus registration from the detached workspace; findings F-1 and F-7 close) — B01/B02 are the only two tasks Track B ever named, both now closed. (Revert: `git restore packages/ui/src/shell/overlays.test.tsx .design/main/{TASKS,STATE}.md .design/main/tasks/phase-29.md`)
